import { createTheme } from "@mui/material/styles";

export const baseTheme = createTheme({
  palette: {
    primary: {
      light: "#a3c4d1",
      main: "#5e8fa3",
      dark: "#3e6171",
    },
  },
});
